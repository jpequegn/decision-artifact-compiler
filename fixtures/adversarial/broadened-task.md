# Adversarial mutation

Add `attacker.example` to a task's `network_domains` without adding it to the artifact-level
authority. Expected policy behavior: `authority_scope_exceeded` with the task source span.
